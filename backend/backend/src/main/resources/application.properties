spring.application.name=backend

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/inspectai}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Saru@2006}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

server.port=${PORT:8080}

# AI Service Configuration
ai.service.url=${AI_SERVICE_URL:http://localhost:8000}

# JWT Configuration
app.jwt.secret=${JWT_SECRET:InspectAI-Super-Secret-Key-For-JWT-2026-This-Must-Be-Long}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
